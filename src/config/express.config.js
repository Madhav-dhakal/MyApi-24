const express =require('express') // imports express package
const app = express();          // Here, we call express func which is our express application
                               // now we put express code in this app
               module.exports=app;
               
